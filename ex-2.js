// Exercise #2: Sum the Numbers
let productPrice = [10, 20, 30, 40, 50];
let total = 0;

for (let i = 0; i < productPrice.length; i++) {
    total += productPrice[i];
};
console.log(`"TotalPrices: ${total}"`);
